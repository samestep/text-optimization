# Text optimization

Prerequisites:

```sh
curl -fsSL https://bun.sh/install | bash
brew install cgal
```

Configure:

```sh
bun install
cmake -DCMAKE_BUILD_TYPE=Release .
```

Build and run:

```sh
./build.sh # pretty fast
./run.sh # pretty slow
```
